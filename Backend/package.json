{
  "name": "count-eat-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.js",
    "build": "node -e \"const fs = require('fs'); const path = require('path'); const dir = './dist'; if (fs.existsSync(dir)) { fs.rmSync(dir, { recursive: true, force: true }); } fs.mkdirSync(dir, { recursive: true }); const copy = (src, dest) => { const stats = fs.statSync(src); if (stats.isDirectory()) { fs.mkdirSync(dest, { recursive: true }); fs.readdirSync(src).forEach(file => { copy(path.join(src, file), path.join(dest, file)); }); } else { fs.copyFileSync(src, dest); } }; fs.readdirSync('./src').forEach(file => { copy(path.join('./src', file), path.join(dir, file)); });\"",
    "start": "node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "browserslist": "^4.24.2",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.5",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.13",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "cpy": "^11.1.0",
    "cpy-cli": "^5.0.0",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
